# Add different file redictions
# Check for null file passed to cmd_setstream (main.c:57)
# fix forks :
    execve does not return on success
	so fds are maybe not properly closed
# abort cmd if invalid file redirection ?